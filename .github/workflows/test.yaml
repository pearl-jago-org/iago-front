name: debug-dns

on:
  workflow_dispatch:

jobs:
  dns:
    runs-on: self-hosted
    steps:
      - name: test connection
        run: java -Djava.net.preferIPv4Stack=true TestConnection http://10.158.248.153:9000